version: 2.1

jobs:
  build:
    working_directory: /services
    docker:
      - image: docker:23.0.1-git
    steps:
      - checkout
      - setup_remote_docker
      run:
        name: Run tests
        command: |
          docker-compose -f ./docker-compose.test.yml up